name: Test

on: [push]

jobs:
  Install:
    runs-on: ubuntu-latest

    steps:
    - uses: Jimver/cuda-toolkit@v0.2.15
      id: cuda-toolkit
      with:
        cuda: '12.4.1'
        method: 'network'
        sub-packages: '["nvcc", "thrust"]'

    - run: nvcc -V
